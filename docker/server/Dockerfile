FROM cvs/python

ARG JANUS_URL
ENV JANUS_URL ${JANUS_URL}

ARG POSTGRES_DSN
ENV POSTGRES_DSN ${POSTGRES_DSN}

ARG SERVER_PORT
ENV SERVER_PORT ${SERVER_PORT}

ARG SDK_API_URI
ENV SDK_API_URI ${SDK_API_URI}

ARG SDK_SIGNAL_URI
ENV SDK_SIGNAL_URI ${SDK_SIGNAL_URI}

WORKDIR /usr/src/cvs

# CMD python -m cvs.server
# EXPOSE ${SERVER_PORT}